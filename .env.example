# Required for PR analysis + browser capture agent (Claude API calls)
ANTHROPIC_API_KEY=

# Referenced by demo-gen.config.json's auth.envVars for logging into the target app.
# Never commit real values here.
DEMO_USER_EMAIL=
DEMO_USER_PASSWORD=
